[package]
name = "double-pendulum"
version = "0.1.0"
edition = "2024"
description = "A double pendulum simulator for the browser"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2.127"

[profile.release]
lto = true
opt-level = "s"
codegen-units = 1
